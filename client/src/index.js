import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

// ReactDOM renders the React application by rendering the App component
ReactDOM.render(<App />, document.getElementById('root'));